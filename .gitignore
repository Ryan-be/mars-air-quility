__pycache__/
*.pyc
*.pyo
.pytest_cache/
data/
.env
certs/
.claude/
.superpowers/
node_modules/

# Built grow firmware wheels (regenerated by scripts/build_grow_wheel.sh)
static/grow_dist/*
!static/grow_dist/.gitkeep
contracts/dist/
grow_unit/dist/
# Built grow firmware wheels for offline / Pi-image use
# (regenerated by scripts/build_local_wheels.sh)
dist/

# Transient PR / agent scratch files — these get generated when prepping
# `gh pr create --body-file` invocations and similar one-off workflows.
# They are not source; keep them out of git.
.pr-body.md
.pr-*.md

